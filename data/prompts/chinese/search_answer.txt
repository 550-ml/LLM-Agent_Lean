你正在从“给定候选列表”中选择可能有用的 mathlib 引理/定义，以辅助证明 Lean 4 定理。

输入：
题目：
{problem}

非形式化描述：
{docstring}

候选项（只能从这里选，禁止编造）：
{theorems}

错误信息（可选）：
{error_message}

选择目标：
选出一组最可能在证明中用到的候选项，用于：
- 证明结构搭建（intro、exists、and/or、forall 等）
- 关键表达式改写/化简（代数、集合运算、类型转换 coercions）
- 常见子目标处理（正性、不等式、有限性/基数、ncard 等）

规则（必须严格遵守）：
1）只能从 {theorems} 候选列表中选择，禁止输出列表外名字。
2）输出 3–10 个，够用即可，宁可少而精。
3）每个选择项必须单独用 <theorem>...</theorem> 包裹。
4）输出名称必须与候选列表完全一致，但如果存在前缀 "Mathlib."，则去掉这个前缀即可（不要随意截断其他模块路径）。
5）除 <theorem> 标签外，不要输出任何解释文字、编号、标点或多余内容。
6）如果 {error_message} 暗示类型不匹配/未知标识符，优先选择与类型转换、simp 改写、重写有关的候选项。

严格输出格式：
<theorem>NAME_1</theorem>
<theorem>NAME_2</theorem>
...