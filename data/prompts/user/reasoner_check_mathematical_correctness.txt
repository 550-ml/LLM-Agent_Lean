You are an expert in mathematics.

Your task is to assess whether the given theorem statement is mathematically sound and consistent with the provided proof sketch. You do NOT need to produce a Lean proof.

Context (proof approach sketch):
{sketch_assembled}

What to check (relaxed criteria):
1. Core mathematical correctness: Look for *fatal* logical errors, incorrect transformations (e.g., invalid substitutions), contradictions, or missing hypotheses that make the statement not provable in principle.
2. Be tolerant of minor omissions: If some routine steps are omitted (algebraic simplifications, standard substitutions, basic domain conditions), do NOT mark it incorrect unless the omission is genuinely essential.
3. Do NOT nitpick helper lemmas: If a helper lemma is generally true or irrelevant to correctness, do NOT flag it. Only flag issues that break the overall argument or the theorem statement itself.
4. Provability relative to the sketch: Decide whether the theorem should be provable using the stated premises and the strategy indicated by the sketch, even if the sketch is incomplete.
5. If the theorem is incorrect, propose the smallest plausible correction (e.g., fix a bound, remove an unintended factor, add a missing nonzero/positivity condition, correct a swapped variable).

Assumptions:
- Treat the provided premises as correct; do NOT re-verify them.
- Treat the Lean syntax as correct; do NOT comment on syntax.

Theorem Statement:
{problem}

Output format:
Return either:
• YES — if the theorem statement is mathematically correct (no fatal issues)
• NO — if the theorem statement has fatal mathematical errors preventing proof

Then provide a brief justification wrapped in:
<justification> ... </justification>

If you answer NO, include:
- what is wrong (at a high level),
- the minimal correction needed,
- and, if relevant, the key missing reasoning steps that would be required after correction.
